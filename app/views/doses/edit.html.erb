<h1>Edit Dose</h1>
<div class="container">
  <h1>Edit  your ingredient and a description </h1>
  <div class="row">
    <div class="col-sm-6">
      <%= simple_form_for([@cocktail, @dose]) do |f| %>
        <%= f.association :ingredient %>
          <%= f.input :description %>
        <%= f.submit "Edit Dose", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
 <%= link_to 'Back', cocktails_path  %>
